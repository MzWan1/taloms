--- src/main/resources/templates/authorities/list.html
+++ src/main/resources/templates/authorities/list.html
@@ -176,6 +176,10 @@
               <small>
                 Click "Add Authority" to register the first one
               </small>
+              <br>
+              <a class="btn btn-sm btn-navy mt-3" th:href="@{/authorities/create}">
+                  <i class="bi bi-plus-lg me-1"></i>[Add Authority]
+              </a>
             </td>
           </tr>
           <tr th:each="auth, stat : ${authorities}"
@@ -231,6 +235,9 @@
           </tr>
           </tbody>
         </table>
+        <div class="card-footer bg-white py-3 border-0" th:if="${authoritiesPage != null and authoritiesPage.totalPages > 1}">
+            <div th:replace="~{fragments/pagination :: pagination(page=${authoritiesPage}, url='/authorities')}"></div>
+        </div>
       </div>
     </div>
   </div>
