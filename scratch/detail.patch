--- src/main/resources/templates/companies/detail.html
+++ src/main/resources/templates/companies/detail.html
@@ -185,6 +185,10 @@
                     <tr th:if="${keys == null or #lists.isEmpty(keys)}">
                         <td colspan="6" class="text-center text-muted py-4">
                             No API keys have been issued for this company.
+                            <br>
+                            <button class="btn btn-sm btn-navy mt-3" type="button" data-bs-toggle="modal" data-bs-target="#generateKeyModal" th:if="${company.active}">
+                                <i class="bi bi-key me-1"></i>[Create API Key]
+                            </button>
                         </td>
                     </tr>
                     <tr th:each="k : ${keys}">
@@ -271,6 +275,9 @@
                     </tbody>
                 </table>
             </div>
+            <div class="card-footer bg-white py-3 border-0" th:if="${usagePage != null and usagePage.totalPages > 1}">
+                <div th:replace="~{fragments/pagination :: pagination(page=${usagePage}, url='/companies/' + ${company.id})}"></div>
+            </div>
         </div>
     </div>
 </div>
